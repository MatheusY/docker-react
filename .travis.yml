language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t matheusy/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true matheusy/docker-react npm run test

